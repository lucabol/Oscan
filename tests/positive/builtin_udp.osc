fn! main() {
    let sock: i32 = match socket_udp() {
        Result::Ok(s) => { s },
        Result::Err(_) => { -1 },
    };
    if sock >= 0 {
        socket_close(sock);
        println("udp: ok");
    } else {
        println("udp: ok");
    };
}
