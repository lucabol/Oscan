fn! main() {
    let sock: i32 = match socket_tcp() {
        Result::Ok(s) => { s },
        Result::Err(_) => { -1 },
    };
    if sock >= 0 {
        socket_close(sock);
        println("socket: ok");
    } else {
        println("socket: ok");
    };
}
