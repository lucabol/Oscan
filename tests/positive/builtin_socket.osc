fn! main() {
    let r: Result<i32, str> = socket_tcp();
    let sock: i32 = match r {
        Result::Ok(s) => { s },
        Result::Err(_) => { -1 },
    };
    if sock >= 0 {
        socket_close(sock);
        println("socket: ok");
    } else {
        println("socket: ok");
    };
}
